export const metadata = {
  title: "Japan 225 · Pre-Open",
  description: "Tokyo direction signal dashboard",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body style={{ margin: 0 }}>{children}</body>
    </html>
  );
}
